# Contributing

## Scope

贡献应围绕安全的医生工作流适配和合成数据研究。不要加入自主诊断、处方、真实电子病历接入、从原始编辑自动学习或真实患者数据。

## Change workflow

1. 说明改动内容、原因以及对安全边界的影响。
2. 为领域行为增加或更新测试。
3. 在离线环境中运行适用的 lint、typecheck 和测试。
4. 保留失败和不利案例，不要为了通过检查删除边界测试。
5. 所有示例、fixture 和截图都必须明确使用合成数据。

## Data handling

不要提交密钥、真实资料、完整模型响应、完整转写、原始音频或本机路径。公开快照应遵守 PUBLICATION.md 中的排除和脱敏规则。
