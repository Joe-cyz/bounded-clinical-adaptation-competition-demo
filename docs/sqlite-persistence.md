# SQLite 持久化

SQLite 当前 migration 为 12。数据库层通过连接、事务和仓储端口向应用层提供能力。

## 事务边界

写入使用明确事务，状态更新、修订指针和审计元数据在同一事务中完成。并发冲突、唯一约束和锁错误会转换为受控应用错误；不会向页面泄露 SQL、路径或底层堆栈。

## 不可变对象

病历修订、资料摄取批次、引用定位和复核快照遵守既定不可变约束。更新只能创建新的版本或决定，不能改写历史内容。

## 数据保留

数据库可保存合成结构化数据和安全元数据，不保存原始音频、完整转写、完整模型响应、密钥或真实患者资料。公开快照不包含运行时数据库。
