# 输入校验规则

所有进入应用服务的病例、病历、资料和参考请求都必须经过服务端校验。

## 基本规则

- 病例必须明确为合成数据；
- 年龄、专科、就诊类型和字段状态必须符合版本化 Schema；
- 必填字段不能用空字符串、空数组或自由文本代替；
- PII 扫描在投影和持久化前执行；
- ID、版本、来源和当前修订必须相互一致；
- 资料引用必须来自允许的证据范围；
- 模型参考不得包含确诊、处方、剂量、患者执行命令或能力承诺；
- public-demo 在读取请求正文、取得数据库或调用外部能力前拒绝写入。

校验结果只返回固定规则 ID、字段路径和安全类别，不回显原始输入。
