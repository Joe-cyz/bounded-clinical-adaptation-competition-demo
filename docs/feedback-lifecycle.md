# 反馈生命周期

反馈流程用于研究治理中的结构化编辑和规则分类，不表示系统会从真实患者或医生操作自动学习。

## 状态

候选修订先经过字段差异、完整性和隐私检查，再进入规则分类。低风险候选可以进入模拟确认，中风险或不确定候选需要人工复核，高风险候选保持拒绝。每次状态变化都保留安全对象 ID、版本和时间元数据。

## 边界

反馈理由不进入公开导出，病历和草稿正文不写入通用审计。确认反馈不会自动改变模型服务、画像或临床流程。
