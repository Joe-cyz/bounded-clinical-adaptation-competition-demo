# 结构化编辑与差异

病历和研究草稿使用结构化字段编辑，不把整段自由文本替换视为安全确认。

## 差异规则

保存前计算字段级差异，区分新增、修改、清空、状态变化和无变化。结果经过 Schema、必填项、PII、并发版本和权限检查。空值不能隐藏必填缺失，修改历史不能覆盖旧修订。

## 医生确认

语音建议、模型参考和资料引用都不能自动改变病历。只有医生在编辑界面明确处理并保存，内容才会进入新的病历修订。
